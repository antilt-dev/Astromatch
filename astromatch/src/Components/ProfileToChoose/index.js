import React from "react";
import {Container} from './styles'

const ProfileToChoose=(props)=>{
  return (
   <Container photoLink={props.photoLink}>

   </Container>
  );
}

export default ProfileToChoose;